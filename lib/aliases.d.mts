/** Type surface for lib/aliases.mjs, which is plain JS so scripts can import it. */
export declare const ALIASES: Record<string, string[]>;
export declare const DIALECT_SYNONYMS: string[][];
export declare function phrasingsFor(term: string): string[];
export declare function synonymLines(): string[];
